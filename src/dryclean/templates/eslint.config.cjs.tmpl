'use strict';

const IGNORED_NUMERIC_LITERALS = [0, 1];
const IMPORT_PLUGIN = require('eslint-plugin-import');
const MAX_PARAMS = 4;

module.exports = [
  {
    plugins: {
      import: IMPORT_PLUGIN,
    },
    languageOptions: {
      sourceType: 'commonjs',
    },
    rules: {
      camelcase: ['error', { properties: 'never' }],
      'consistent-return': 'error',
      'import/no-duplicates': 'error',
      'import/order': [
        'error',
        {
          groups: ['builtin', 'external', 'internal'],
          'newlines-between': 'always',
        },
      ],
      'max-params': ['error', { max: MAX_PARAMS }],
      'no-console': 'error',
      'no-empty': ['error', { allowEmptyCatch: false }],
      'no-magic-numbers': ['error', { ignore: IGNORED_NUMERIC_LITERALS, enforceConst: true }],
      'no-restricted-syntax': [
        'error',
        {
          selector: 'ImportDeclaration',
          message: 'Use require() instead of import. This is a CommonJS project.',
        },
      ],
      'no-unused-vars': ['error', { vars: 'all', args: 'after-used' }],
      quotes: ['error', 'single', { avoidEscape: true }],
    },
  },
];
